#include <iostream>

using namespace std;

int main()
{
    double a=5, b=9, sum, dif, x, y;
    sum=(a*a)+(b*b);
    dif=(a*a)-(b*b);
    x=(a*a)*(b*b);
    y=(a*a)/(b*b);
    
    cout<<sum<<endl<<dif<<endl<<x<<endl<<y;
}